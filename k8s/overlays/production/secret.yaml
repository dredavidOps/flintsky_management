# PRODUCTION: Use External Secrets Operator or Sealed Secrets instead!
# This is a placeholder - DO NOT commit real secrets to git
apiVersion: v1
kind: Secret
metadata:
  name: flintsky-secrets
  namespace: flintsky
type: Opaque
stringData:
  # Use: kubectl create secret generic flintsky-secrets \
  #   --from-literal=SECRET_KEY=$(openssl rand -base64 50) \
  #   --from-literal=DATABASE_PASSWORD=<your-db-password> \
  #   --namespace=flintsky
  SECRET_KEY: "CHANGE_ME_IN_PRODUCTION"
  DATABASE_PASSWORD: "CHANGE_ME_IN_PRODUCTION"
